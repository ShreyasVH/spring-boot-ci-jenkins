services:
  spring-boot-ci-jenkins-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "1536:1536"
    volumes:
      - .:/app
      - /app/target
    container_name: spring-boot-ci-jenkins-dev